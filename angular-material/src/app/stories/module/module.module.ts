export enum sizes {
  small = 'small',
  medium = 'medium',
  large = 'large',
}

export enum descriptionSizeTresTipos {
  description = 'maneja 3 tipos de tamaños, al momento de importar el componente viene con un tamaño por defecto (medium), por medio del atributo "size" puede modificar el tamaño tanto como',
}
export enum descriptionSizeDosTipos {
  description = 'maneja 2 tipos de tamaños, al momento de importar el componente viene con un tamaño por defecto (medium), por medio del atributo "size" puede modificar el tamaño tanto como `SMALL` O `MEDIUM`',
}
